#!/bin/bash
set -e
# Second pass with the feature on; the registry is not built at all otherwise.
RUSTFLAGS="-D warnings" cargo check --all-targets
RUSTFLAGS="-D warnings" cargo check --all-targets --features exfiltrate
